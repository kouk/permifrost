version: "1.0"

# Databases
databases:
    - testdb:
        owner: testuser
        shared: no
# Roles
roles:
    - testrole:
        owner: testuser

# Users
users:
    - testname:
        owner: testuser
        can_login: yes

# Warehouses
warehouses:
    - testwarehouse:
        owner: testuser
        size: x-small
