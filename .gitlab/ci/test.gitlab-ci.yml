.test:
  stage: test
  image:
    name: $CI_REGISTRY_IMAGE:$CI_COMMIT_SHA
    entrypoint: [""]
  before_script:
    - pip install '.[dev]'

lint:
  extends: .test
  before_script:
    - pip install black
  script:
    - make show_lint

pytest:
  extends: .test
  script:
    - pytest -v --cov-report= --cov meltano_permissions -m "$PYTEST_MARKERS"
    - coverage combine .coverage
    - coverage report
    - coverage html
  artifacts:
    paths:
      - htmlcov/
    when: on_success